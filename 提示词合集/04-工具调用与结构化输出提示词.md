# 04 工具调用与结构化输出提示词

## 1. 标准工具数据转报告提示词

**源码位置**：`tradingagents/agents/utils/google_tool_handler.py:733`

**用途**：用于 Google 模型工具调用兼容处理：当工具数据已经获取后，统一要求生成报告。

```text
现在请基于上述工具获取的数据，生成详细的 {analyst_type} 报告。

股票信息：
- 公司名称：{company_name}
- 股票代码：{ticker}

分析要求：
1. 报告必须基于工具返回的真实数据进行分析
2. 包含具体的数值和专业分析
3. 提供明确的投资建议和风险提示
4. 报告长度不少于 800 字
5. 使用中文撰写
6. 确保在分析中正确使用公司名称 "{company_name}" 和股票代码 "{ticker}"

{specific_requirements}

请生成专业、详细的 {analyst_type} 报告。
```

## 2. 初始分析请求

**源码位置**：`tradingagents/graph/propagation.py:30`

**用途**：图启动时放入 `messages` 的第一条 HumanMessage。

```text
请对股票 {company_name} 进行全面分析，交易日期为 {trade_date}。
```

## 3. 结构化交易信号抽取

**源码位置**：`tradingagents/graph/signal_processing.py:69`

**用途**：从交易员/风控的自然语言报告中提取结构化决策。

```text
您是一位专业的金融分析助手，负责从交易员的分析报告中提取结构化的投资决策信息。

请从提供的分析报告中提取以下信息，并以 JSON 格式返回：

{
    "action": "买入/持有/卖出",
    "target_price": 数字({currency}价格，必须提供具体数值，不能为 null),
    "confidence": 数字(0-1之间，如果没有明确提及则为0.7),
    "risk_score": 数字(0-1之间，如果没有明确提及则为0.5),
    "reasoning": "决策的主要理由摘要"
}

请确保：
1. action 字段必须是 "买入"、"持有" 或 "卖出" 之一，绝对不允许使用英文 buy/hold/sell
2. target_price 必须是具体数字，应该是合理的 {currency} 价格数字（使用 {currency_symbol} 符号）
3. confidence 和 risk_score 应该在 0-1 之间
4. reasoning 应该是简洁的中文摘要
5. 所有内容必须使用中文，不允许任何英文投资建议

特别注意：
- 股票代码 {stock_symbol or '未知'} 是 {market_info['market_name']}，使用 {currency} 计价
- 目标价格必须与股票的交易货币一致（{currency_symbol}）

如果某些信息在报告中没有明确提及，请使用合理的默认值。
```

## 4. 工具绑定角色列表

**源码位置**：`tradingagents/graph/trading_graph.py:_create_tool_nodes`

**用途**：这些不是提示词本身，但决定 LLM 可调用哪些量化/新闻工具。

```text
market:
- get_stock_market_data_unified
- get_YFin_data_online
- get_stockstats_indicators_report_online
- get_YFin_data
- get_stockstats_indicators_report

social:
- get_stock_sentiment_unified
- get_stock_news_openai
- get_reddit_stock_info

news:
- get_stock_news_unified
- get_global_news_openai
- get_google_news
- get_finnhub_news
- get_reddit_news

fundamentals:
- get_stock_fundamentals_unified
- get_finnhub_company_insider_sentiment
- get_finnhub_company_insider_transactions
- get_simfin_balance_sheet
- get_simfin_cashflow
- get_simfin_income_stmt
- get_china_stock_data
- get_china_fundamentals
```

## 5. 工具调用防死循环规则

**源码位置**：`tradingagents/graph/conditional_logic.py`

**用途**：不直接发给 LLM，但控制“工具调用 -> 报告生成 -> 清理消息 -> 下一节点”的流程。

```text
核心规则：
- 如果分析报告已经生成并且长度超过阈值，则结束当前分析师节点。
- 如果最后一条 AIMessage 有 tool_calls，则进入对应 tools 节点执行工具。
- 如果工具调用次数达到上限，则强制结束当前节点，避免死循环。
- 基本面分析师工具调用上限更严格，一般一次调用即可获取所有数据。
```

