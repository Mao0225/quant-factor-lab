# 独立研究与选股系统 v1 验收记录

日期：2026-09-21。依据统一设计 v1 及引用文档；采用已选第一张浅色蓝色视觉稿。结论：500股原型的整体流程实现并通过本次验收；数据尚不满足正式历史投资效果论证，实验没有证明PPO优势。

## 交付与入口

启动：在项目根目录运行 `python -m closed_loop_research.app --port 8767`，打开 http://127.0.0.1:8767 。服务仅监听本机。代码主要在 `closed_loop_research/app/`，原有内核经契约核验复用，旧系统及旧实验保留。启动说明见 `v1_runbook.md`；实施歧义与分阶段计划见 `v1_implementation_plan.md`。

已实现不可变数据快照、协议与白名单校验、组合搜索、配对交易增量奖励、真实PPO更新、批次检查点与暂停恢复、V选择和独立T测试、冻结模型与元数据分离、人工开放/停用、先完整评分再过滤、方案修订、不可变结果、贡献解释、CSV，以及研究/选股两个工作区的六页和辅助页面。异步计算采用单训练队列，避免全局随机状态相互干扰。企业使用端不在本次范围。

## 真实数据

数据快照：`raw500_75b3494c10edbcd9`，位于 `workspace/system_v1/datasets/`。从约19.8GB原始TSV流式读取500股所需50个源字段，共601,000行、1,202个交易日，范围2021-06-18至2026-06-16；保存全部441个字段的目录，构造24个因果价量信号。源文件SHA256：`3544905ab46772f29bc18f6b4e1109b793d6d1ab4e45b2cda0a394a90917f3d4`。完整成功导入耗时约330.7秒；更早一次扫描因非法证券代码失败，修复后重跑，不能称仅扫描过一次源文件。

源数据含640行非法/空证券代码，记录后排除；选定500股没有补造行情行。121个Parquet文件经指纹核验。详情见 `v1_source_audit.md`。

实际协议扣除预热后按时间划分：

| 用途 | 日期 |
|---|---|
| F：组合拟合 | 2021-12-15—2023-09-21 |
| E：候选奖励 | 2023-09-22—2024-12-10 |
| V：模型选择 | 2024-12-11—2025-08-26 |
| T：冻结后历史测试 | 2025-08-27—2026-06-16 |

T标注为已观察历史，不能称全新未观察留出集。评分一致性验证只读取F/E；前一日收盘信号用于随后交易日，因此评分证据中的信号日期可以早于分区首个成交日。

财务披露时点发现未来信息：InfoPublDate晚于交易日382,953行；另一披露索引已知430,890行中未来131,131行，其余170,110行未知。无法从当前宽表恢复历史修订版本，财务不进入历史训练或过滤。

最新5个交易日的500股换手率全部为0，但存在实际成交量，当前换手率过滤禁用。金额单位、板块字典、行业、市值尚无充分证据，均未开放；可用附加过滤为上市自然日数。不可用属性在新结果中为null并附原因，不展示成可靠的0。旧方案含禁用条件时阻止运行，要求显式调整。

固定500股选择偏差、复权和公司行动证据缺口、停牌/涨跌停成交近似仍存在。账本机制测试通过不代表这些历史输入已完成正式金融审计。源文件最新日为2026-06-16，页面明确显示历史日期和过期提示。

## 自动测试与真实研究

- `python -m pytest closed_loop_research/tests -q --basetemp closed_loop_research/workspace/v1_test_final69 --junitxml closed_loop_research/docs/v1_validation.xml`：69通过，0失败、0跳过，约45.4秒。
- `node closed_loop_research/tests/test_frontend_config.cjs`：通过，覆盖提交时读取实际表单、固定配置副本、禁用旧条件不得被静默丢弃。
- `node --check closed_loop_research/app/static/app.js`：通过。
- 测试覆盖时间隔离、费用与持仓账本、PPO与恢复、协议约束、任务与接口、模型身份、评分/过滤顺序、版本固定、结果持久化及数据质量降级。没有用新实验覆盖旧产物。

项目：`a2b020efa0544bab8679cee8655ba640`。

真实对照：`955160065d2d42069e8080da892c2753`，路径 `workspace/system_v1/comparisons/955160065d2d42069e8080da892c2753/suite/`。A固定因子、B随机生成、C单因子IC奖励、E交易增量奖励，种子7和19，共8次完整运行。生成组采用每组2批、每批4候选、每侧组合搜索Q=6；全部训练后统一V选择，全部冻结后统一T测试。

审计产物 `suite/v1_acceptance.json`：48候选、8次PPO更新、6次质量失败、16个合法负奖励、6个正奖励（均属于C组），重新核算42个非失败奖励、校验192个回测产物。基础设施异常与质量失败区分处理。

本轮池提交变化为0，8个实验都保留初始三因子权重0.4/0.3/0.3，V/T表现相同。小预算只证明机制能执行，不证明PPO有效，也不能说明PPO永久无效。负结果与原始轨迹完整保存。

另一个独立页面操作实验 `47d2820804ac4a6f919f9c4be7e84beb` 已完成暂停、恢复、2批8候选和2次PPO更新，自动产生V候选；它的T仍未发起，界面不会伪造T回测。8组对照已覆盖完整T流程。

冻结模型A-seed-7与E-seed-7分别与研究评分核对6个F/E日期、每次全部500股：得分、贡献、完整排名和缺失标记一致，实际模型数值最大误差0。另有嵌套119日依赖诊断，误差上限4.45e-16。证据为 `v1_real_scoring_parity.json` 与 `v1_real_scoring_parity_E_seed_7.json`。

## 页面与交接验收

浏览器实际完成：数据中心和字段原因、研究协议、启动/暂停/恢复、五个实验页签、回测净值和逐日持仓/订单证据、候选模型详情、人工设为可用、带过滤Top N选股、股票因子贡献、方案创建/加载/新修订、历史结果复用和CSV入口。CSV接口核对10条数据、前导零代码、日期、具体模型版本及过滤配置。最终两个浏览器页面控制台没有记录error。

真实E回测页面展示2023-09-22至2024-12-10的292个交易日及资产净值，首日信号为2023-09-21；数据来自已存回测产物，不现场重算或编造未运行T。

模型开放与选股操作放在 `workspace/v1_ui_acceptance/` 隔离目录，使用正式数据和真实冻结A-seed-7模型。正式目录的9个版本仍为candidate，正式方案/选股结果为空。当前隔离验收模型最终处于disabled，用于验证停用后的计算失败。

有效选股结果 `result_2f42ea4081b949f6bf56e3f7090d1535`：实际日期2026-06-16，完整范围500、基础资格500、评分覆盖500，上市满7000自然日后208只，取Top 10。股票解释显示真实标准化值和加权贡献。方案新修订 `plan_43e2e23be6cc4b639f3c43e63c2419be` 保留旧修订、不固定数据快照，下次按原模型版本使用兼容的新快照。

早期隔离结果 `result_7da701f58d874f9c8096ea652e409e87` 使用了后续发现异常的换手率字段；它只作为发现问题的历史测试记录保留，不作为过滤正确性的证据。修复后旧条件无法重算，用户显式清除并保存上述新修订。

停用后的失败任务 `92cc915216ec448a91a65e8d4816153b` 被后台拒绝；浏览器仍显示前次有效结果、208只过滤数和10条名单，未覆盖历史。验收中同时修复提交时漏读最新表单值的问题，并增加回归覆盖。

最终持久状态断言和浏览器观察摘要保存为 `workspace/system_v1/ui_acceptance.json`。验收用8768服务已关闭；正式8767服务保持运行，验收目录和原始证据保留。

模型交接规则：系统生成候选后，在模型管理查看原型限制、V/T证据和数据就绪信息，再人工“设为可用”。此后选股工作台可选择具体版本。程序不会自动切换模型、放宽条件、重新标准化或将原模型收益套给自定义条件。

## 用户授权后的模型开放

同日后续用户要求提供一两个模型、允许生成描述并明确同意所需确认。按该授权开放E组种子7的真实冻结版本，命名为“500股价量组合 · 原型一号”，补充三因子权重、实验来源、使用顺序及数据限制说明。其余8个版本维持候选。由于各版本目前因子和权重相同，只开放一个代表版本；没有按T表现重新选优，也没有将重复模型包装成不同策略。

正式目录已成功生成该模型的默认Top 50结果，全范围500股评分后取50只，实际日期2026-06-16。开放前后的冻结权重一致；用户授权、具体版本、任务和结果ID记录在 `workspace/system_v1/model_release_v1.json`。上文“正式目录无名单、全部候选”是初次隔离验收时点的记录，当前状态以本节为准。
