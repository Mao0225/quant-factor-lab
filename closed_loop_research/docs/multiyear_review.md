# 多年升级独立审查

审查范围：history/features、总历史依赖与语法掩码、准备后行情账本、标准化/排名缓存、压缩证据、进程对照、历史T标识及报告。由独立只读审查者检查，未让审查者直接修改实现。

1. **派生字段掩码死路（已修复后启动实验）。** 仅return_20字段、历史上限120时，旧掩码会允许delay_60(delay_60(...))，但后续无法放入字段。新增动作后剩余槽位的最小叶子依赖检查，并为必需字段保留可行槽位。复现测试先失败后通过；审查者确认原死路被禁止。默认含原始字段的多年协议未受该特定死路影响。

2. **对照清单重入类型误报。** 序列化后tuple变为list，直接Python对象比较会误报身份变化。修复用规范JSON摘要比较；仍保留配置、快照、代码、组与种子身份拒绝规则。

3. **默认重入遗失已有T汇总。** 当T已经完成、不带final-test重入时，原路径标记解锁却漏写T指标。修复为校验并保留已有冻结T结果；没有结果时仍不解锁。摘要报告实际拥有T结果的种子数量。回归覆盖未解锁重入、首次解锁、默认重入及报告生成。

后两项位于实验编排层，不影响本次首次执行带final-test的路径。为不在运行期间更换代码身份，修复先在workspace/reentry_fix独立副本实现并验证，待实跑结束再回迁。原运行完整源码已归档versions/multiyear_round1，摘要33310d2273161e5e0fba4c8b661142344e5ccbe2101b62126011ed7f67738565。

没有发现当前默认多年协议中的新增前视或快速账本语义差异。这个结论不等同于对旧源数据复权、静态股票池或真实成交条件完成审计。
